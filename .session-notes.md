# Session Notes

**Date**: 2026-06-10
**Session summary**: MXP_scheduler 전용 세션 (RTL 무변). 코드리뷰→주변부 수정 후, config 파일 기반 HW 파라미터 자동 도출(hwconfig)을 spec→plan→subagent-driven 7-task로 구현하고 PR #2로 main에 머지했다.

## What We Did

1. **MXP_scheduler 코드리뷰 + 수정** (`ec194ec`, main 직커밋): 연산 로직 버그 없음 확인. 수정: coeffs 오타 키 거부(HW), `evaluate()`가 `_blocks`/`dram_bits`를 1회만 계산해 공유(기존 6회 중복 walk), CLI ValueError→`p.error`, `report()` 헤더에 freq_ratio/eff_bw, ceil-divide 정리, 테스트 55→58, README 코드맵 라인번호 갱신.
2. **hwconfig 구현** (branch `feat/hwconfig`, 12 commits → PR #2 머지, main `d21e9ba`):
   - Task 1: CACTI 7 빌드 (`third_party/cacti`, gitignored) + `docs/cacti-setup.md`
   - Task 2: `dram_presets.json` 4종 — pj/bit 문헌 검증 (Ha Stanford thesis 2018 Fig 4.8/4.9 PDF 직접 판독, O'Connor MICRO-50 2017 Fig 1a). LPDDR5 5.0 / LPDDR5X 4.5 / DDR4 15.0 / DDR5 10.0
   - Task 3~5: `hwconfig.py` — `load_config`(unknown-key 에러)/`dram_params`/`cacti_run`(캐시)/`resolve`(coeffs 3단 합성: DEFAULT→auto→config)
   - Task 6: 트윈 양쪽 CLI `--config` — 명시 플래그 > config > 기본값, `--coeffs` 최종 승리. crosscheck에 word_bits=16 parity 케이스 추가
   - Task 7: 실 CACTI 통합 테스트(skipif) + README 사용 가이드(사용 순서/키 표/주의사항)
   - 매 Task spec-compliance + code-quality 2단 리뷰 (전부 Opus 서브에이전트) + 최종 전체 리뷰. 리뷰 픽스: CACTI exit code 표면화, 캐시 자가복구, 테스트의 실캐시 오염 제거(backup/restore fixture), example config turnkey화

## Current State

- main `d21e9ba`, 트리 clean (CLAUDE.md/.session-notes.md 핸드오프 커밋만 남음), 원격 동기화 완료. `feat/hwconfig` 브랜치 삭제됨.
- 게이트: `cd MXP_scheduler && python -m pytest -q` → **81 passed** · `--selftest` OK · `--crosscheck` OK.
- 실측 e2e: `--config hw_config.example.json` → freq_ratio 0.078125, onchip 0.0323 pJ/bit (CACTI 22nm), eff_bw 409.6 bits/on-chip-cycle.
- CACTI 빌드는 로컬 전용(gitignored) — 새 환경에서는 `docs/cacti-setup.md` 따라 재빌드 필요. 없으면 hwconfig 테스트 1개만 skip, 나머지 80개는 무관.

## Decisions & Rationale

| Decision | Why |
|----------|-----|
| Ramulator 2.0 v1 제외 | 타이밍 시뮬레이터라 에너지 미출력 + 우리 stall 모델은 순수 bandwidth라 꽂을 자리 없음. peak BW/클럭은 표준명에서 도출. derate 캘리브레이션 필요 시 재고려 |
| CACTI 실설치+subprocess (테이블 내장 대신) | 임의 SRAM 구성에 항상 실제 값. 캐시로 1회 비용화 |
| chip_freq는 config 명시, CACTI는 검증용 경고 | 칩 클럭은 로직 timing closure가 정하는 값 |
| CACTI cfg full 키셋 | 최소 cfg는 10분+ hang 또는 SIGFPE (실측). `_cacti_cfg` docstring에 기록 — DRAM/NUCA 키 정리 금지 |
| LPDDR=device-internal, DDR=I/O포함 기준 혼재 허용 | 한 칩 안의 매핑 랭킹엔 무관. README에 계열 간 절대값 비교 금지 명시 |
| mac/rmw 계수 기본값 유지 | CACTI/datasheet 범위 밖. 매핑-상수항이라 랭킹 무영향. 실측 시 config로 주입 |

## Open Questions

- DRAM effective-BW derate (row miss/RW turnaround/refresh, 통상 peak의 70~90%) — 정밀도 올릴 때 Ramulator 1회 캘리브레이션?
- 실제 모델의 wbits 맵 (양자화 결과) → `--bits-file`로 연동하는 플로우 미구축.
- (이월) report-only 항목 업스트림 반영 시점 — CLAUDE.md 2026-06-04 섹션 HIGH/MEDIUM 목록.

## Next Session: Start Here

> CLAUDE.md 상단 kickoff (2026-06-10) 참조. 스케줄러를 더 갈 거면 derate 캘리브레이션/프리셋 확장/wbits 연동, RTL로 돌아가면 2026-06-04 섹션의 보고-only HIGH/MEDIUM 업스트림 항목부터.

## TODO

- [ ] (선택) MXP_Tools 보고-only 항목: `examples/01_smoke.py` SystemExit, `test_gemm.py` prec_B 회귀 방어, viz NaN/rmw_gen MAX_N
- [ ] (선택) 스케줄러 derate factor 캘리브레이션 spec / 실 워크로드 wbits 맵 연동
- [ ] (선택) production dataflow / Vivado timing closure / 9-mode+mixed CI 자동화 (기존 후보)
