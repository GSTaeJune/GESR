# Session Notes

**Date**: 2026-06-04
**Session summary**: 프로젝트 전체에 멀티에이전트 파일단위 코드리뷰→수정 루프를 돌려 1회 수렴(88 agents, 21건 수정/15파일). 수정분을 main 에 커밋(`8a0e673`)하고 9-mode 통합 + mixed-sweep 으로 회귀 0 확인. 연산 로직 버그는 없었고, 고친 건 전부 빌드·실행 스크립트/테스트 게이트/데드코드 등 주변부.

## What We Did
- 멀티에이전트 워크플로(리뷰 에이전트 → 시니어 triage → 수정 에이전트 → 재검토)를 백그라운드로 실행, 57파일 리뷰 후 1라운드 수렴.
- triage 결과: 15 accepted / 20 report-only / 23 rejected. 수정 21건 적용.
- 시니어 에이전트가 검증된 데이터패스 변경(int_to_fp32 클램프, fp32_adder 파이프라인)은 PASS 회귀 위험으로 거절 — 보수적 게이트 정상 동작.
- 적용 후 직접 재검증: sim/tests 10 PASS, MXP_Tools 53 PASS, shell 9개 bash -n OK.
- main 에 직접 커밋(브랜치 안 땀, 사용자 지시) → `8a0e673`.
- 회귀 확인 sweep: 통합 9-mode `ALL 9 MODES PASSED`, mixed `ALL 3 MIXED MODES PASSED`.

## Current State
- 작업트리: 코드 수정분 커밋 완료. CLAUDE.md / .session-notes.md 만 미커밋(이 핸드오프 커밋 대상).
- 검증 상태: 통합 9-mode + mixed-sweep + 단위 pytest 전부 PASS. 회귀 0.
- 유일한 RTL 변경 GEMM.v 는 데드넷(sc_bcast) 제거로 동작 무변, sweep 으로 확인됨.

## Decisions & Rationale
| Decision | Why |
|----------|-----|
| 자동수정 범위 = new/+tb/+sim/ 만 | CLAUDE.md 규칙: imports/vendored/MXP_Tools(fork) in-place 수정 금지 |
| imports/MXP_Tools/vendored 는 report-only | 업스트림/fork 정책상 별도 반영 필요 |
| 검증된 데이터패스 변경 거절 | 9-mode/mixed PASS 회귀 위험 > latent 가드 이득 |
| main 직접 커밋(브랜치 X) | 사용자 명시 지시 |

## Open Questions
- report-only 항목들을 언제/어떻게 업스트림(../MXP, ../sram, MXP_Tools fork)에 반영할지.
- 보류된 timing-closure 단계(int_to_fp32 클램프, fp32_adder 파이프라인 분할)를 다음에 다룰지.

## Next Session: Start Here
> 회귀까지 끝났음. 다음은 (a) report-only 항목 업스트림 반영, 또는 (b) production dataflow 최적화 / Vivado timing closure / CI 자동화 중 사용자가 고르는 방향. 보고-only 전체 목록은 CLAUDE.md 상단 kickoff 의 "보고-only 항목" 참조.

## TODO
- [ ] (선택) report-only HIGH: `MXP_Tools/examples/01_smoke.py` SystemExit 버그 업스트림 수정
- [ ] (선택) report-only MEDIUM: `test_gemm.py` prec_B/K-block 커버리지 보강 (64× A=2 버그 회귀락)
- [ ] (선택) report-only: viz.py NaN vmin/vmax, rmw_gen.py MAX_N 가드, imported PE 8-bit 슬라이스
- [ ] (선택) production dataflow 최적화 / Vivado timing closure / 9-mode+mixed CI
